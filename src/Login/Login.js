import React, {Component} from 'react';

const login = "Login To Reaction";

class Login extends Component{
    render(){
        return (
            <h1 className="AppTitle">{login}</h1>
        )
    }
}

export default Login;